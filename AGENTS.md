<!-- deno-fmt-ignore-file -->

# リポジトリガイドライン

## プロジェクト構成とモジュール

このリポジトリは、バンドル処理を行わないフラットな構成のマニフェストV3形式Chrome拡張機能です。
エントリーポイントは`manifest.json`です。
`background.js`は設定画面を開きます。
`content.js`はゲーム画面のアーティファクト一覧とクリック位置を読み取り、スコアを計算して設定画面へ送信します。ゲーム画面のDOMは変更しません。
`artifact-list-xhr-bridge.js`はページコンテキストでゲームのアーティファクト一覧XHR通信を監視します。
`core/`には、画面やChrome APIに依存しない共有ロジックを配置します。
`score-config-core.js`はユーザー設定の検証と正規化を担当します。
`score-config-editor-core.js`は設定画面のルール編集、並び替え、検索に使う共有ロジックを担当します。
`artifact-score-core.js`は採点とツールチップ用の整形を担当します。
`artifact-list-core.js`は一覧応答の検証とDOM要素との対応付けを担当します。
`tests/`には、Denoで実行する自動テストを配置します。
共有ロジックのテストは、`core/`の実装と同じベース名を使用します。
同梱JSONとマニフェストのテストも、対象ファイルと同じベース名を使用します。
設定UIは`options.html`、`options.css`、`options.js`で構成されています。
同梱データは`default-user-config.json`と`effects-master.json`に保存されています。
大規模な機能追加によってディレクトリ分割が明確に必要にならない限り、新しい実行時ファイルはルートに置いてください。
ブラウザから読み込むアセットは`manifest.json`に登録してください。

## ビルド・テスト・開発コマンド

- `mise install`で、`mise.toml`に指定されたDenoをインストールします。
- `deno fmt`でリポジトリ内の対応ファイルを整形します。
- `deno fmt --check`で、ファイルを変更せずにフォーマットを確認します。
- `deno lint`でJavaScriptの静的解析を実行します。
- `deno test`で自動テストを実行します。

コンパイル工程はありません。
ローカル開発では`chrome://extensions`を開いてデベロッパーモードを有効にし、**パッケージ化されていない拡張機能を読み込む**からこのリポジトリを選択してください。
コードを変更した後は拡張機能を再読み込みしてください。
コンテンツスクリプトの動作を確認するときは、開いているグランブルーファンタジーのページも再読み込みしてください。

## コーディングスタイルと命名規則

Denoの標準フォーマッターとリンターを基準とします。
JavaScriptでは既存のスタイルに従い、ダブルクォート、セミコロン、変数・関数の`camelCase`、共有定数の`UPPER_SNAKE_CASE`を使用してください。
HTMLのIDとCSSクラスには、`rule-form`や`status-message`のようなケバブケースを使用してください。
Chrome拡張機能APIは適切な箇所で非同期に呼び出してください。
ページコンテキストでの通信監視、コンテンツスクリプトでの表示処理、設定画面の状態管理という既存の役割分担を維持してください。

## テスト方針

Denoの標準テストランナーを使用しますが、カバレッジ基準は設定していません。
DOMやChrome APIへ依存しないロジックは、対応する単体テストを追加してください。
ブラウザ固有のDOM更新やChrome API連携は、過度に模倣せず手動確認します。
変更を提出する前に`deno fmt --check`、`deno lint`、`deno test`を実行してください。
マニフェストエラーやコンソールエラーを発生させずに、拡張機能を読み込めることも手動で確認してください。
設定画面の一覧プレビューでのスコア表示とクリック位置の同期に加え、ルール追加・編集、インポート・エクスポート、リセット、設定の永続化を確認してください。
不具合修正では、再現手順と確認手順をプルリクエストに記載してください。

## コミットとプルリクエスト

Git履歴には現時点で`init`しかなく、確立されたコミット規約はありません。
`Add weapon-specific score rules`のように、簡潔な命令形の件名を使用してください。
各コミットの変更範囲は絞ってください。
プルリクエストには、動作変更の概要、手動確認の内容、関連Issueへのリンクを記載してください。
設定画面の表示を変更した場合はスクリーンショットを添付してください。
ホスト権限、保存形式、同梱JSONデータを変更した場合は明記してください。
