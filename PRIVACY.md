# プライバシーポリシー

最終更新日: 2026年8月10日

GBF Artifact
Scorer（以下「本拡張機能」）は、ユーザーのプライバシーを尊重します。
本ポリシーでは、本拡張機能が取り扱う情報、その利用目的、保存方法について説明します。

## 取り扱う情報

本拡張機能は、次の情報をユーザーの端末内で取得・処理します。

### アーティファクト一覧情報

本拡張機能は、グランブルーファンタジーのゲーム画面がすでに取得したアーティファクト一覧の通信結果を監視し、アーティファクトの識別情報、効果、クオリティ、属性、武器種、レアリティ、お気に入り・不用品の状態および関連するメタデータを処理します。

これらの情報は、スコアの計算と設定画面への表示にのみ使用します。通信結果そのものを`chrome.storage.local`などへ永続的に保存することはなく、ゲーム画面を閉じるか再読み込みすると破棄されます。

計算済みのスコア、一覧上の位置、スコア内訳、お気に入り・不用品の状態および選択位置は、設定画面との同期に必要な範囲だけ`chrome.storage.session`へ一時的に保持します。この一時データはディスクへ永続化されず、ブラウザーの再起動、拡張機能の無効化、再読み込みまたは更新によって削除されます。

本拡張機能は、アーティファクト一覧の通信を識別する目的で、対象となるリクエストのURLと応答状態を端末内で確認します。閲覧履歴の作成、保存または外部送信は行いません。

### ユーザー設定

本拡張機能は、ユーザーが入力または変更した次の設定を取り扱います。

- 採点ルールとスコア
- 効果、クオリティ、属性および武器種の条件
- 組み合わせボーナス
- お気に入りの加算スコア
- 任意で入力されたコメント
- スコアのしきい値と強調表示設定

これらの設定は、次回利用時に復元するため、Chromeの`chrome.storage.local`を使用してユーザーのブラウザープロファイル内に保存します。Googleアカウントを利用したブラウザー間同期は行いません。

設定のJSON読み込みを行った場合、その内容はほかのユーザー設定と同様に端末内へ保存されます。JSON書き出しで作成されたファイルはユーザー自身が管理するものであり、本拡張機能や開発者のサーバーへアップロードされません。

## 情報の利用目的

取り扱う情報は、次の目的にのみ使用します。

- ユーザーが設定したルールに基づくアーティファクトの採点
- 設定画面の一覧プレビューへのスコア、内訳、お気に入り・不用品の状態および選択位置の表示
- ユーザー設定の保存、復元、読み込みおよび書き出し

ゲーム操作の自動化やゲームサーバー上のデータ変更には使用しません。

## 外部送信、共有および販売

本拡張機能は、ユーザーのアーティファクト情報、設定、閲覧情報その他のユーザーデータを、開発者のサーバーまたは外部サービスへ送信しません。

本拡張機能による追加のネットワーク通信、アクセス解析、広告配信はありません。ユーザーデータを第三者へ共有、販売または貸与することもありません。

ゲーム本体が通常の動作として行う通信は、本拡張機能による追加通信ではありません。

## 保存期間と削除方法

アーティファクト一覧の通信結果はゲーム画面内のメモリで一時的に処理され、永続的には保存されません。計算済みのスコア、一覧上の位置、スコア内訳、お気に入り・不用品の状態および選択位置は、設定画面との同期のためブラウザーセッション中だけメモリに保持されます。

ユーザー設定は、ユーザーが変更するか、本拡張機能の保存データまたは本拡張機能自体をChromeから削除するまで、`chrome.storage.local`に保存されます。設定画面から採点ルールの削除や標準設定への復元を行うこともできます。

JSON書き出しによって作成されたファイルは、ユーザーが端末上で削除できます。

## 権限の使用目的

本拡張機能は、次の権限とサイトアクセスを使用します。

### `storage`

`chrome.storage.local`は、採点ルール、組み合わせボーナスおよび表示設定をブラウザー内に保存し、次回利用時に復元するために使用します。

`chrome.storage.session`は、計算済みのスコア、一覧上の位置、スコア内訳、お気に入り・不用品の状態および選択位置を設定画面と同期するため、ブラウザーセッション中だけメモリに保持する目的で使用します。

### `https://game.granbluefantasy.jp/*`へのアクセス

対象サイト上でコンテンツスクリプトを実行し、ゲーム画面が取得したアーティファクト一覧情報を端末内で採点して、設定画面の一覧プレビューへ表示するために使用します。ゲーム画面のDOMは一覧の並び順、属性、武器種、お気に入り・不用品の状態および選択位置を確認する目的で読み取るだけで、要素、属性またはスタイルの追加や変更は行いません。他のウェブサイトにはアクセスしません。

## Chrome Web Storeの限定利用要件

本拡張機能によるユーザーデータの利用は、本ポリシーに記載した単一の目的に限定されます。ユーザーデータを、単一の目的と関係のない用途、信用力の判断、融資、広告、販売または第三者への転送に使用しません。

本拡張機能による情報の利用は、Chrome Web
Storeユーザーデータポリシー（限定利用要件を含みます）に準拠します。

## 本ポリシーの変更

本拡張機能の機能または情報の取り扱いを変更した場合、本ポリシーを更新し、最終更新日を改定します。重要な変更がある場合は、Chrome
Web Storeの掲載情報または本拡張機能の画面でお知らせします。

## お問い合わせ

本ポリシーに関するお問い合わせは、GitHub Issuesからお願いします。

https://github.com/eggpan/gbf-artifact-scorer/issues

## 非公式ツールについて

本拡張機能は非公式ツールであり、株式会社Cygamesおよびグランブルーファンタジーの運営元とは関係ありません。
